<h1>Create a New Post</h1>
<%= form_for @post do |f| %>
	<p>
		<%= f.lable :title %><br/>
		<%= f.text_area :content %>
	</p>
	<p>
		<%= f.lable :content %><br/>
		<%= f.text_area :content %>
	</p>
	<p>
		<%= f.submit "Create a New Post" %>
	</p>
<% end %>